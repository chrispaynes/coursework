require_relative 'enigma/key'

class Enigma
  def encrypt (message, key, date)
    # get key rotations
    # get date rotations
    #

     key_rotations = Key.new(rotation_number)
    
    message
  end
end
